{
  "name": "whatsapp-webhook",
  "version": "1.0.0",
  "description": "Lambda function for WhatsApp Business webhook",
  "main": "src/index.js",
  "scripts": {
    "test": "jest",
    "build": "esbuild src/index.js --bundle --platform=node --target=node20 --outfile=dist/index.js",
    "deploy": "npm run build && terraform -chdir=../../infra/terraform/modules/whatsapp apply"
  },
  "dependencies": {
    "@aws-sdk/client-secrets-manager": "^3.490.0",
    "@aws-sdk/client-sns": "^3.490.0",
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "esbuild": "^0.19.0",
    "jest": "^29.7.0"
  }
}
